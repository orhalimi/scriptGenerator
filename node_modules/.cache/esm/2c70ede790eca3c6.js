let Splitter;_90e‍.w("./splitter",[["default",["Splitter"],function(v){Splitter=v}]]);const json = require('./tamplates/example.json');


function createExeFiles(config, input) {
    // try {
        const splitter = new Splitter(config)
        splitter.splitTasks()
        _90e‍.g.console.log(splitter.individualTasks);
        for (let gId in splitter.taskGraphs){
            _90e‍.g.console.log(gId + ";")
            _90e‍.g.console.log(splitter.taskGraphs[gId].serialize())
        }

    // } catch (e) {
    //     console.log('error:' + e)
    // }
}

createExeFiles(json, ['x'])